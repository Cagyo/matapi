/**
 * Flow engine — Phase 2 stub. Will evaluate sensor → action automation rules
 * stored in the `flows` table once Phase 2 begins.
 */
export class FlowEngine {
  async evaluate(): Promise<void> {
    // no-op
  }
}
