/**
 * Phase 1 setup wizard placeholder.
 * Standalone HTTP server on :3000 — runs once, writes .env + features.json,
 * then exits.
 */
console.log('Setup wizard not yet implemented (Phase 1).');
process.exit(0);
